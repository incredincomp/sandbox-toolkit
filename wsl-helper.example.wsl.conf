; Recommended starting point for a dedicated sandbox-toolkit WSL helper distro.
; Apply manually inside the helper distro after review.

[automount]
enabled=false

[interop]
enabled=false
appendWindowsPath=false
